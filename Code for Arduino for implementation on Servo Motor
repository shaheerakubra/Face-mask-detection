#include <Servo.h> Servo myservo;  int LED = 13; char data = 1; void setup() 
{ 
myservo.attach(9);   Serial.begin(9600);   pinMode(LED,OUTPUT);   digitalWrite(LED,LOW); 
} 

void loop() 
{ 
if(Serial.available()) 
  { 
    data=Serial.read(); 
  } 
  if(data=='1') 
  { 
myservo.write(90); 
   // delay(1000); 
Serial.println("LED turned on");   Serial.println("ITS WORKINGGG"); a  } 
                          else if(data == '0')   { 
myservo.write(0); digitalWrite(LED,LOW);     Serial.println("LED turned off"); 
  } 
}  
